spring:
  config:
    activate:
      on-profile: local

  data:
    redis:
      host: localhost
      port: 6379
      password:

---
spring:
  config:
    activate:
      on-profile: dev

  data:
    redis:
      host:
      password:
      port: